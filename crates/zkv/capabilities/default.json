{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline core permissions for the main window. App-defined commands need no ACL entry.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
