{
  "name": "zkv-gui-assets",
  "private": true,
  "version": "0.0.0",
  "description": "Build + typecheck tooling for the zkv web UI. NOT required for `cargo build`; the committed js/*.js, api.js, and forms.js are what the binary embeds via include_bytes!. Node is only needed to regenerate them (scripts/build-gui-assets.sh) or to type-check.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "esbuild": "0.24.2",
    "typescript": "5.6.3"
  }
}
