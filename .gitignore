/target
/zec_sqlite_wallet*

# Generated by tauri-build (desktop feature); platform-specific ACL schemas.
/crates/zkv/gen/schemas

# Third-party license bundle generated at build time (build/license_bundle.rs)
# into OUT_DIR; ignore any stray copy under the asset tree.
/crates/zkv/src/gui/assets/licenses.txt

# Guard against accidental commits of the `local` and `tmp` paths
/local
/tmp

# Frontend (zkv gui) dev tooling, installed by `npm ci` in the assets dir
# to type-check + regenerate the committed JS. Not needed for `cargo build`.
/crates/zkv/src/gui/assets/node_modules
